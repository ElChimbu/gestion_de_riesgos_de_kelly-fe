import React from 'react';

// Deprecated: This component was removed. Keeping a stub to avoid build issues if referenced mistakenly.
const StyleTest: React.FC = () => null;

export default StyleTest;